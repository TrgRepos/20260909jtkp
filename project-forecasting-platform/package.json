{
  "name": "project-forecasting-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo training target for Cursor AI-Powered Engineering Program - Level 3 (Session 3.1 / 3.2). Two modules (users, profiles) sharing a small shared/ library, plus a db/ directory of raw SQL migrations used by the Postgres MCP lab.",
  "main": "src/server.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "ts-node src/server.ts",
    "test": "jest --runInBand",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "overrides": {
    "qs": "^6.16.0",
    "glob": "^13.0.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jest": "^30.0.0",
    "@types/node": "^20.14.2",
    "@types/supertest": "^7.2.1",
    "jest": "^30.5.1",
    "supertest": "^7.2.2",
    "ts-jest": "^29.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
